---
name: feature
description: Implement a multi-layer feature.
---
<!-- GENERATED by nbp-forge from forge/recipes/feature.md — do not edit here; edit the recipe/brick and run `npx nbp-forge build`. -->
# Feature

Implement the feature across all required layers.

### Working folder

At the start, create this run's folder:

```bash
RUN_DIR=$(run-dir feature "$DESC" --track)
```

The logic (slug, timestamp, tracking) lives in the script — the skill does **not** restate it.

Break it into verifiable steps and validate each layer.
