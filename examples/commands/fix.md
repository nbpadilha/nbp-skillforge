---
name: fix
description: Fix a small, well-scoped bug.
---
<!-- GENERATED by nbp-forge from forge/recipes/fix.md — do not edit here; edit the recipe/brick and run `npx nbp-forge build`. -->
# Fix

Fix the bug with a small scope.

### Working folder

At the start, create this run's folder:

```bash
RUN_DIR=$(run-dir fix "$DESC" --prefix fix --track)
```

The logic (slug, timestamp, tracking) lives in the script — the skill does **not** restate it.

Apply the minimal change and validate.
