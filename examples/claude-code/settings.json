{
  "//": "EXAMPLE — merge into YOUR project's .claude/settings.json. Opt-in, Claude Code only.",
  "//2": "Stops the agent from hand-editing an nbp-skillforge GENERATED file (edit the recipe instead).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          { "type": "command", "command": "node .claude/hooks/forge-guard.mjs" }
        ]
      }
    ]
  }
}
